version: '3.8'

services:
  grassvision:
    build: .
    ports:
      - "8042:8042"
    env_file:
      - .env
    volumes:
      - ./config:/app/config
      - ./logs:/app/logs
    restart: unless-stopped
